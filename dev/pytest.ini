[pytest]
# Ignore the archive directory completely
norecursedirs = Archive .git .pytest_cache venv node_modules

# Asyncio settings
asyncio_mode = strict

# Test discovery and display settings
addopts = -v

# Default timeout for tests
timeout = 300